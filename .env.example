# Voice Trainer — 獨立 API Keys（全部用新帳號，不與其他專案共用）
# 複製這個檔案為 .env 並填入你的 keys

# ── Groq (https://console.groq.com) ──────────────────────────────────────────
# 免費，每天 14,400 reqs，最快的推理速度
# 兩個獨立 key：Llama 和 Qwen3 各自不共用配額
VOICE_GROQ_API_KEY=gsk_YOUR_GROQ_KEY_FOR_LLAMA
VOICE_GROQ_QWEN_API_KEY=gsk_YOUR_GROQ_KEY_FOR_QWEN3

# ── Cerebras (https://cloud.cerebras.ai) ─────────────────────────────────────
# 免費，wafer-scale 超快推理
VOICE_CEREBRAS_API_KEY=csk_YOUR_NEW_CEREBRAS_KEY_HERE

# ── NVIDIA NIM (https://build.nvidia.com) ────────────────────────────────────
# 免費，llama-3.3-70b-instruct，申請後取得 nvapi-xxx key
VOICE_NVIDIA_API_KEY=nvapi_YOUR_NEW_NVIDIA_KEY_HERE

# ── OpenRouter (https://openrouter.ai) ───────────────────────────────────────
# 免費 tier（:free 後綴 model），多模型多樣性
VOICE_OPENROUTER_API_KEY=sk-or-v1-YOUR_NEW_OPENROUTER_KEY_HERE

# ── Ollama (本地，選用) ───────────────────────────────────────────────────────
# 完全免費，私密，不需帳號。安裝：https://ollama.ai
# 安裝後執行：ollama pull qwen2.5:7b
# VOICE_OLLAMA_URL=http://localhost:11434/v1
# VOICE_OLLAMA_MODEL=qwen2.5:7b

# ── Neon PostgreSQL (https://neon.tech) ──────────────────────────────────────
# 免費，cloud DB，PWA/Android sync 用
VOICE_DATABASE_URL=postgresql://user:pass@host/neondb?sslmode=require

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3005
NODE_ENV=development
